# Rule: Always Run Dev Server After Coding

Whenever you write or modify code in this project, always ensure the local development server is running (`npm run dev`) at the end of your changes so the user can immediately test and view the updates locally.
